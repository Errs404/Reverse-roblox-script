local function td(be)
	if not be then
		return nil
	end
	local ej, fo = next(be)
	if not fo then
		return nil
	end
	local te = next(be, ej)
	if te then
		error("Expected at most child, had more than one child.", 2)
	end
	return fo
end
return td
